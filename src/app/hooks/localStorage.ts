'use client'

import { useEffect } from "react"

export const cartItems = [{}];


// (function(){

//     useEffect(() => {
//         for (let i = 0; i < localStorage.length; i++) {
//             const itemName = String(localStorage.key(i))
//             const itemQty = Number(localStorage.getItem(itemName))
//             cartItems.push({name: itemName, qty: itemQty})
//         }
//     })

// })()

// cartItems.shift()



